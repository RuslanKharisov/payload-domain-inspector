# Domain summary

_Auto-generated by payload-domain-inspector_

## Product

Поля:

- `name`: string — field
- `sku`: string — field
- `shortDescription`: string | null (optional) — field
- `productCategory`: ProductCategory (optional) — relation
- `productImage`: Media (optional) — field
- `images`: | {
        image?: (number | null) | Media;
        id?: string | null;
      }[]
    | null (optional) — field
- `brand`: Brand (optional) — relation
- `layout`: (ContentBlock | MediaBlock | CallToActionBlock)[] | null (optional) — field
- `slug`: string | null (optional) — field
- `sku_normalized`: string | null (optional) — field

## ProductCategory

Поля:

- `title`: string — field
- `title_en`: string | null (optional) — field
- `description`: string | null (optional) — field
- `parent`: ProductCategory (optional) — relation
- `breadcrumb`: string | null (optional) — field
- `depth`: number | null (optional) — field
- `isPromoted`: boolean | null (optional) — field
- `productCount`: number | null (optional) — field
- `slug`: string | null (optional) — field

## Brand

Поля:

- `name`: string — field
- `description`: string | null (optional) — field
- `isPromoted`: boolean | null (optional) — field
- `productCount`: number | null (optional) — field
- `slug`: string | null (optional) — field

## Category

Поля:

- `title`: string — field
- `slug`: string | null (optional) — field
- `parent`: Category (optional) — relation
- `breadcrumbs`: | {
        doc?: (number | null) | Category;
        url?: string | null;
        label?: string | null;
        id?: string | null;
      }[]
    | null (optional) — field

## User

Поля:

- `username`: string — field
- `roles`: ('super-admin' | 'admin' | 'user' | 'content-editor')[] | null (optional) — field
- `tenants`: | {
        tenant: number | Tenant;
        roles: ('tenant-admin' | 'tenant-viewer')[];
        id?: string | null;
      }[]
    | null (optional) — field
- `email`: string — field
- `resetPasswordToken`: string | null (optional) — field
- `resetPasswordExpiration`: string | null (optional) — field
- `salt`: string | null (optional) — field
- `hash`: string | null (optional) — field
- `_verified`: boolean | null (optional) — field
- `_verificationToken`: string | null (optional) — field
- `loginAttempts`: number | null (optional) — field
- `lockUntil`: string | null (optional) — field
- `sessions`: | {
        id: string;
        createdAt?: string | null;
        expiresAt: string;
      }[]
    | null (optional) — field
- `password`: string | null (optional) — field
- `collection`: 'users' — field

## Tenant

Поля:

- `slug`: string | null (optional) — field
- `name`: string — field
- `domain`: string | null (optional) — field
- `requestEmail`: string — field
- `warehouse`: Warehouse (optional) — relation
- `allowPublicRead`: boolean | null (optional) — field
- `accountDetailsSubmitted`: boolean | null (optional) — field
- `apiUrl`: string | null (optional) — field
- `apiToken`: string | null (optional) — field
- `apiType`: ('google' | 'erp' | 'custom') | null (optional) — field
- `inn`: string | null (optional) — field
- `status`: ('ACTIVE' | 'LIQUIDATING' | 'LIQUIDATED' | 'BANKRUPT' | 'REORGANIZING') | null (optional) — field
- `layout`: (CallToActionBlock | ContentBlock | MediaBlock | ArchiveBlock | FormBlock)[] | null (optional) — field
- `meta`: {
    title?: string | null;
    /**
     * Maximum upload file size: 12MB. Recommended file size for images is <500KB.
     */
    image?: (number | null) | Media;
    description?: string | null;
  } (optional) — field

## Warehouse

Поля:

- `tenant`: Tenant (optional) — relation
- `title`: string — field
- `warehouse_address`: Address — relation
- `selectedAddressData`: | {
        [k: string]: unknown;
      }
    | unknown[]
    | string
    | number
    | boolean
    | null (optional) — field
- `capacity`: number | null (optional) — field

## Address

Поля:

- `fias_id`: string — field
- `kladr_id`: string | null (optional) — field
- `city`: string | null (optional) — field
- `region`: string | null (optional) — field
- `street`: string | null (optional) — field
- `house`: string | null (optional) — field
- `fullAddress`: string | null (optional) — field
- `geo_lat`: string | null (optional) — field
- `geo_lon`: string | null (optional) — field
- `country_iso_code`: string | null (optional) — field

## Icon

Поля:

- `label`: string — field
- `key`: string — field
- `url`: string | null (optional) — field
- `thumbnailURL`: string | null (optional) — field
- `filename`: string | null (optional) — field
- `mimeType`: string | null (optional) — field
- `filesize`: number | null (optional) — field
- `width`: number | null (optional) — field
- `height`: number | null (optional) — field
- `focalX`: number | null (optional) — field
- `focalY`: number | null (optional) — field
- `sizes`: {} (optional) — field

## Stock

Поля:

- `tenant`: Tenant (optional) — relation
- `quantity`: number — field
- `product`: Product — relation
- `price`: number | null (optional) — field
- `currency`: Currency — relation
- `condition`: ('НОВЫЙ С ЗАВОДА' | 'НОВЫЙ ИЗЛИШЕК' | 'Б/У' | 'ВОССТАНОВЛЕН') | null (optional) — field
- `expectedDelivery`: string | null (optional) — field
- `warranty`: number | null (optional) — field
- `warehouse`: Warehouse (optional) — relation
- `title_in_admin`: string | null (optional) — field
- `_city`: string | null (optional) — field
- `_region`: string | null (optional) — field
- `isPromoted`: boolean | null (optional) — field

## Currency

Поля:

- `code`: string — field
- `name`: string — field
- `symbol`: string | null (optional) — field

## CompanyProject

Поля:

- `tenant`: Tenant (optional) — relation
- `name`: string — field

## CompanyCertification

Поля:

- `tenant`: Tenant (optional) — relation
- `name`: string — field

## CompanyPost

Поля:

- `tenant`: Tenant (optional) — relation
- `title`: string — field
- `slug`: string — field
- `content`: {
    root: {
      type: string;
      children: {
        type: any;
        version: number;
        [k: string]: unknown;
      }[];
      direction: ('ltr' | 'rtl') | null;
      format: 'left' | 'start' | 'center' | 'right' | 'end' | 'justify' | '';
      indent: number;
      version: number;
    };
    [k: string]: unknown;
  } — field

## Subscription

Поля:

- `tenant`: Tenant (optional) — relation
- `startDate`: string — field
- `endDate`: string | null (optional) — field
- `tariff`: Tariff — relation
- `status`: ('active' | 'inactive' | 'pending_payment' | 'expired') | null (optional) — field

## Tariff

Поля:

- `name`: string — field
- `price`: number — field
- `description`: string | null (optional) — field
- `benefits`: | {
        value: string;
        id?: string | null;
      }[]
    | null (optional) — field
- `features`: ('CANT_ANY' | 'CAN_MANAGE_STOCK' | 'CAN_CREATE_POSTS' | 'CAN_PROMOTE_PRODUCTS')[] — field

## CompanyType

Поля:

- `name`: string — field

## Click

Поля:

- `companyId`: string | null (optional) — field
- `tenant`: Tenant (optional) — relation
- `src`: 'web' | 'ai' | 'telegram' | 'email' — field
- `ctx`: string | null (optional) — field
- `query`: string | null (optional) — field
- `target`: string — field
- `ip`: string | null (optional) — field
- `userAgent`: string | null (optional) — field

## Redirect

Поля:

- `from`: string — field
- `to`: {
    type?: ('reference' | 'custom') | null;
    reference?:
      | ({
          relationTo: 'pages';
          value: number | Page;
        } | null)
      | ({
          relationTo: 'posts';
          value: number | Post;
        } | null);
    url?: string | null;
  } (optional) — field
